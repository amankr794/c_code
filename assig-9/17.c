// Write a function power ( a, b ) to calculate the value of a raised to b
/*
#include<stdio.h>
#include<conio.h>
// function declaration
int power(int, int);
// function definition
int power(int a, int b)
{
    int mul, i;
    for (i = mul = 1; i <= b; i++)
        mul = mul * a;
    return mul;
}
// main function
void main()
{
    int num, Pow;
    printf("Enter the number and power: ");
    scanf("%d%d", &num, &Pow);
    int x = power(num, Pow);
    printf("The value of %d raised to %d is %d",num,Pow,x);
    getch();
}
*/